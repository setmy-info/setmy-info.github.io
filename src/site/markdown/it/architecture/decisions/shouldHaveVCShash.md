# Should have VCS (GIT) commit hash in software logs

Hash should be logged out.
APP UI should have version logged into console and can be seen somewhere (as comment, or in footer, or info
view/panel, ...).
