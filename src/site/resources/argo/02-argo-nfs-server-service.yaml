apiVersion: v1
kind: Service
metadata:
    name: argo-nfs-server
    namespace: argo
spec:
    selector:
        app: argo-nfs-server
    ports:
        -   name: nfs
            port: 2049
            targetPort: 2049
    clusterIP: None
