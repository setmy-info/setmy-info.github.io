apiVersion: v1
kind: Service
metadata:
    name: nfs-server
    namespace: argo
spec:
    selector:
        app: nfs-server
    ports:
        -   name: nfs
            port: 2049
            targetPort: 2049
    clusterIP: None
